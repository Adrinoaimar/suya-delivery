<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
      Un recurso de `values-v27` REEMPLAZA por completo al de `values`: no se fusionan.
      Por eso cada estilo repite aquí su `parent` y todos sus atributos; si solo se
      declarara el añadido de API 27, `AppTheme.NoActionBar` heredaría de forma implícita
      de `AppTheme` (`Theme.AppCompat.Light.DarkActionBar`) y la app volvería a mostrar
      la barra de acción azul del tema por defecto sobre la cabecera propia.
    -->

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@color/suya_ivory</item>
        <item name="android:statusBarColor">#EEF6F1</item>
        <item name="android:navigationBarColor">#F0F7F3</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@drawable/splash</item>
        <item name="android:statusBarColor">#0B7048</item>
        <item name="android:navigationBarColor">#0B7048</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
