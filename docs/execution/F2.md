# Checkpoint F2 — Supabase inicial

## Resultado

- CLI y cliente Supabase fijados a versiones exactas.
- Proyecto local aislado con `project_id = "suya-delivery"`.
- Migración inicial: catálogo, membresías, riders, pedidos, snapshots de líneas, pagos, GPS,
  incidentes, notificaciones y auditoría.
- RLS activa en todas las tablas expuestas y grants explícitos para Data API 2026.
- Hashes de entrega/cancelación viven en `private.order_secrets`, nunca en Realtime/Data API.
- Roles de restaurante y columnas actualizables limitados; riders suspendidos no se autoactivan.
- CI ejecuta pgTAP y lint SQL con Supabase local.

## Evidencia local

- Typecheck, lint y 49 pruebas: pasan.
- Escaneo: 188 archivos, sin patrones de secreto.
- `git diff --check`: pasa.
- SQL local: bloqueado únicamente porque Docker/Podman no está instalado.

## Revisión multi-modelo

- UltraCode/Opus fue invocado, pero no produjo salida antes del timeout.
- Kimi K3 xhigh detectó exposición de hashes y permisos amplios; ambos fueron corregidos.

## Siguiente acción

Push/PR para ejecutar la base en CI; luego adaptadores Supabase y separación de builds.
