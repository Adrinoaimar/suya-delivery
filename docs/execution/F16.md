# F16 — Auditoría de preparación de despliegue

Fecha: 9 de septiembre de 2026 (`America/Lima`)

## Veredicto

**not-ready para publicación completa; código y artefactos listos, gates externos pendientes.**

## Evidencia aprobada

- `main` actual: `5c82228`; frontend post-merge `34414533346` terminó `success`.
- Base de datos `34413494281`, Android `34413494247`, E2E y simulador iOS: `success`.
- APK debug y release unsigned disponibles; no hay keystore de producción.
- Supabase live responde; cuatro restaurantes y catálogo real verificados.
- Build, typecheck, lint, tests, bundles, secret scan y `git diff --check` pasan en CI/local.

## Gates pendientes

- Cloudflare: los tres `pages.dev` responden `200`, pero sirven bundles anteriores. Workflow requiere
  `CLOUDFLARE_API_TOKEN`; el repositorio solo tiene publishable key.
- Google: `/auth/v1/settings` devuelve `external.google=false`. Faltan Client ID/Secret y redirect URLs
  autorizadas en Google Cloud/Supabase.
- Edge Function de cuentas: código y workflow manual están en `main`; falta desplegar con
  `SUPABASE_ACCESS_TOKEN`, `SUPABASE_SERVICE_ROLE_KEY` y `ALLOWED_ORIGINS`.
- Operación: faltan correos autorizados de propietarios; Donde Joel mantiene `accepting_orders=false`.
- Distribución: APK release requiere keystore custodio del propietario y prueba física.

## Primeras acciones tras recibir secretos

1. Ejecutar workflow manual de Edge Functions.
2. Configurar Google Auth y probar PKCE en Android/web.
3. Configurar variables Cloudflare y ejecutar release con previews/rollback.
4. Cargar correos autorizados, invitar propietarios y activar cuentas.
5. Firmar APK, instalar en dispositivo físico y ejecutar smoke final.
