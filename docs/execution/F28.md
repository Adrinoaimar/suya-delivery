# F28 — Hardening OTA, CSP y checkpoint de artefactos

Fecha: 2026-09-15 (`America/Lima`)

## Objetivo

Cerrar el bloque de actualización móvil opcional sin convertirlo en requisito del checkout, eliminar declaraciones de tracking no usadas y dejar artefactos Android reproducibles e identificables.

## Cambios

- `src/lib/liveUpdate.ts`: validación estricta de origen/ruta, bundle ID, ZIP, checksum, firma y `content-type`; timeout de 10 s con `AbortController`; cualquier error mantiene abierta la aplicación empaquetada.
- `scripts/prepare-mobile-live-update.mjs`: base HTTPS exacta de Suya y bundle ID seguro; la clave privada no se imprime.
- `public/_headers`: se elimina GA/GTM de CSP, se autoriza solo el origen del manifest OTA y se fijan `Content-Type`/cache para `latest.json` y los ZIP.
- `tests/mobile-live-update.test.ts` y `tests/security-hardening.test.ts`: regresión focal OTA/CSP.

## Evidencia

- Focal OTA/CSP: **7/7**.
- Suite Vitest: **68 archivos / 301 pruebas**.
- `npm run typecheck`: pasa.
- `npm run lint`: pasa.
- `npm run build`: pasa.
- `npm run verify:apps`: pasa; bundles customer, rider y backoffice aislados.
- `npm run test:e2e`: pasa **12/12** sobre previews separados de customer, rider y backoffice; HTTP 200, ruta/encabezado esperado, sin overflow, controles sin nombre ni `pageerror`.
- `npm run security:secrets`: pasa; 911 archivos sin patrones de secreto.
- `npm audit --omit=dev --audit-level=high`: 0 vulnerabilidades.
- `verify:production`: rechaza correctamente la ausencia de configuración productiva y no realiza despliegue.
- Endpoint consultado: HTTP 200 con HTML, no manifest JSON; el cliente lo rechaza y no se habilitó OTA.
- APKs debug: ZIP íntegro, un firmante y APK Signature Scheme v2 válido; versión `1.4`, código `5`.
- La preparación del emulador temporal no avanzó: `sdkmanager` no pudo descargar los catálogos remotos y el SDK aislado no contiene emulator/system image. No se modificó el sistema ni se falsificó smoke nativo.

## Seguimiento de headers — `ac9b607`

Se eliminó la duplicación entre la regla wildcard y las reglas específicas: CORS queda definido una sola vez para `/mobile-updates/*`; `latest.json` fija `application/json` y `no-store`; los ZIP fijan `application/zip` y caché inmutable. La regresión OTA/CSP permanece en **7/7**, typecheck, lint y `git diff --check` pasan.

## Artefactos

- `output/android/Suya-Rider-debug.apk` — 25,371,041 bytes — SHA-256 `4a430701ce30c497e8b63c3d415770bd078e5bebf527577c208e979687a7db4a` — `com.suya.rider`.
- `output/android/Suya-Backoffice-debug.apk` — 25,238,844 bytes — SHA-256 `390b0fb0baf2f0127853e2d49684bcac7a86f32c7ac2d717e96aceade516a388` — `com.suya.backoffice`.
- `output/android/Suya-Wallet-Observer-debug.apk` — 25,191,944 bytes — SHA-256 `020fe42f548e89b5d04e6792a8fd1c16f097b740453d46124c753c75f9239244` — `com.suya.walletobserver`.

Los APK son de prueba y no se versionan. No contienen firma release ni autorizan pagos reales.

## Bloqueos y siguiente comando

El flujo oficial `supabase db lint/test` necesita un daemon/puerto local accesible. Las pruebas SQL independientes no sustituyen ese gate. También faltan dispositivo, firma release, endpoint vial privado y autorización/configuración de pagos. El primer comando tras resolver el runtime SQL es:

```sh
npm run db:start && npm run db:lint && npm run db:test
```

Después repetir la suite global, `npm run build:mobile:roles`, smoke nativo y la matriz de pagos sin usar datos reales.

## Seguimiento posterior — guardia de evidencia y cola Android (2026-09-15)

- Se añadió `supabase/migrations/20260915140000_wallet_evidence_reuse_guard.sql`: trigger `SECURITY DEFINER` con `pg_advisory_xact_lock` que serializa la identidad visible y rechaza que una evidencia ya verificada vuelva a autorizar otro intento. El caso queda para revisión manual, incluso si llega desde otro dispositivo u operador.
- La prueba `supabase/tests/20260914230000_atomic_wallet_checkout_and_receiver_binding.test.sql` ahora cubre trigger, duplicado multiequipo, rollback del intento/evidencia, receptor inactivo y la API de ingesta (24 aserciones declaradas).
- Se corrigió `YapeNotificationListenerService`: bajo el límite de 500 se conservan el evento entrante y todos los pendientes antes del historial sincronizado. Android `test` pasa **5/5**.
- Se añadió `supabase/migrations/20260915150000_active_wallet_receiver_guard.sql`: un binding o una observación nueva no puede apuntar a una cuenta receptora desactivada; el histórico permanece disponible para auditoría.

Las migraciones nuevas aún requieren `npm run db:test` mediante el runtime oficial accesible; el intento del 2026-09-15 volvió a recibir `ECONNREFUSED 127.0.0.1:54322`. No se ejecutó pago real, despliegue, publicación ni instalación en dispositivo.
