# F28 — Hardening OTA, CSP y checkpoint de artefactos

Fecha: 2026-09-15 (`America/Lima`)

## Objetivo

Cerrar el bloque de actualización móvil opcional sin convertirlo en requisito del checkout, eliminar declaraciones de tracking no usadas y dejar artefactos Android reproducibles e identificables.

## Cambios

- `src/lib/liveUpdate.ts`: validación estricta de origen/ruta, bundle ID, ZIP, checksum, firma y `content-type`; timeout de 10 s con `AbortController`; cualquier error mantiene abierta la aplicación empaquetada.
- `scripts/prepare-mobile-live-update.mjs`: base HTTPS exacta de Suya y bundle ID seguro; la clave privada no se imprime.
- `public/_headers`: se elimina GA/GTM de CSP, se autoriza solo el origen del manifest OTA y se fijan `Content-Type`/cache para `latest.json` y los ZIP.
- `tests/mobile-live-update.test.ts` y `tests/security-hardening.test.ts`: regresión focal OTA/CSP.

## Evidencia

- Focal OTA/CSP: **7/7**.
- Suite Vitest: **68 archivos / 306 pruebas**.
- `npm run typecheck`: pasa.
- `npm run lint`: pasa.
- `npm run build`: pasa.
- `npm run verify:apps`: pasa; bundles customer, rider y backoffice aislados.
- `npm run test:e2e`: pasa **12/12** sobre previews separados de customer, rider y backoffice; HTTP 200, ruta/encabezado esperado, sin overflow, controles sin nombre ni `pageerror`.
- `npm run security:secrets`: pasa; 913 archivos sin patrones de secreto.
- `npm audit --omit=dev --audit-level=high`: 0 vulnerabilidades.
- `verify:production`: rechaza correctamente la ausencia de configuración productiva y no realiza despliegue.
- Endpoint consultado: HTTP 200 con HTML, no manifest JSON; el cliente lo rechaza y no se habilitó OTA.
- APKs debug: ZIP íntegro, un firmante y APK Signature Scheme v2 válido; versión `1.4`, código `5`.
- La preparación del emulador temporal no avanzó: `sdkmanager` no pudo descargar los catálogos remotos y el SDK aislado no contiene emulator/system image. No se modificó el sistema ni se falsificó smoke nativo.

## Seguimiento de headers — `ac9b607`

Se eliminó la duplicación entre la regla wildcard y las reglas específicas: CORS queda definido una sola vez para `/mobile-updates/*`; `latest.json` fija `application/json` y `no-store`; los ZIP fijan `application/zip` y caché inmutable. La regresión OTA/CSP permanece en **7/7**, typecheck, lint y `git diff --check` pasan.

## Artefactos

- `output/android/Suya-Rider-debug.apk` — 25,371,081 bytes — SHA-256 `e5dd3c76abd9deb1bd906a631e14a563aeb1e35324c7f080480a267971729bbe` — `com.suya.rider`.
- `output/android/Suya-Backoffice-debug.apk` — 25,239,372 bytes — SHA-256 `838da484c2b88b586e431870c5e229d8b0da5e8b434174e18e8d7a5b4131171c` — `com.suya.backoffice`.
- `output/android/Suya-Wallet-Observer-debug.apk` — 25,191,988 bytes — SHA-256 `9ef9ee7fe546870e1c562181c870148849becfc9073ad9f29e122bfebb475a42` — `com.suya.walletobserver`.

Los APK son de prueba y no se versionan. No contienen firma release ni autorizan pagos reales.

## Bloqueos y siguiente comando

El flujo oficial `supabase db lint/test` necesita un daemon/puerto local accesible. Las pruebas SQL independientes no sustituyen ese gate. También faltan dispositivo, firma release, endpoint vial privado y autorización/configuración de pagos. El primer comando tras resolver el runtime SQL es:

```sh
npm run db:start && npm run db:lint && npm run db:test
```

Después repetir la suite global, `npm run build:mobile:roles`, smoke nativo y la matriz de pagos sin usar datos reales.

## Seguimiento posterior — guardia de evidencia y cola Android (2026-09-15)

- Se añadió `supabase/migrations/20260915140000_wallet_evidence_reuse_guard.sql`: trigger `SECURITY DEFINER` con `pg_advisory_xact_lock` que serializa la identidad visible y rechaza que una evidencia ya verificada vuelva a autorizar otro intento. El caso queda para revisión manual, incluso si llega desde otro dispositivo u operador.
- La prueba `supabase/tests/20260914230000_atomic_wallet_checkout_and_receiver_binding.test.sql` ahora cubre trigger, duplicado multiequipo, rollback del intento/evidencia, receptor inactivo, controles de dispositivo y la API de ingesta (38 aserciones declaradas).
- Se corrigió `YapeNotificationListenerService`: bajo el límite de 500 se conservan el evento entrante y todos los pendientes antes del historial sincronizado. Android `test` pasa **5/5**.
- Se añadió `supabase/migrations/20260915150000_active_wallet_receiver_guard.sql`: un binding o una observación nueva no puede apuntar a una cuenta receptora desactivada; el histórico permanece disponible para auditoría.
- Se añadió `supabase/migrations/20260915160000_wallet_observer_device_controls.sql`: revocación/reactivación, rotación de token de un solo retorno, auditoría sin `token_hash`, localización por UUID para tokens nuevos y límite de ingesta por dispositivo. La UI de Backoffice expone `Revocar`, `Reactivar` y `Rotar token`; el mapper conserva `device_active` al listar o rotar.
- Se corrigió U-10 en `src/app/AppShell.tsx`: el enlace de salto conserva un objetivo táctil mínimo de 44 px al recibir foco. La medición web actual confirma 44 px en los tres bundles; la regresión focal pasa 5/5.

Las migraciones nuevas aún requieren `npm run db:test` mediante el runtime oficial accesible; el intento del 2026-09-15 volvió a recibir `ECONNREFUSED 127.0.0.1:54322`. La regresión SQL declara 38 aserciones. No se ejecutó pago real, despliegue, publicación ni instalación en dispositivo.

## Seguimiento posterior — declaración manual y revisión tardía (2026-09-15)

- Commit local: `a960237` (`fix(security): reject anonymous payment claims`), sobre `137c1e6` (`fix(payments): preserve declared manual reviews`).
- `PaymentInstructions` ahora separa «Ya pagué»/«Aún no pagué», bloquea renovación con estado desconocido o declarado, permite registrar pagador alterno y oculta QR/reintentos para `refunded`; el código o el nombre solo son datos de búsqueda para caja.
- `20260915170000_payment_claims_and_late_review.sql` agrega claims, HMAC privado, declaración con autorización de usuario/token invitado, propuesta tardía limitada a 24 h, receptor exacto, verificación manual final y auditoría sin hashes/códigos. Sus ramas autenticadas rechazan explícitamente `auth.uid() IS NULL`. Mantiene las firmas de RPC existentes y delega `submit_payment_evidence`.
- `SupabaseWalletObserverService` y Backoffice distinguen hora de notificación (`observed_at`) de hora de recepción server-side (`created_at`); ninguna se presenta como hora bancaria.
- Suite global: **68/312**; focal payment/service/UI: **33/33**; typecheck, lint, build, secretos (**915 archivos**), audit (**0 vulnerabilidades**), bundles aislados y smoke E2E **12/12** pasan.
- Android `test`/`assembleDebug`: pasan con Java 21/SDK 36. APKs debug trazadas a `137c1e6`: Rider **25,371,365 bytes**, SHA-256 `779adb9578e375479e73732e4d38c2877bb07b51a154c1313f1ce96131cbc83c`; Backoffice **25,239,780 bytes**, SHA-256 `b7bd499c2d3a1ecd591d8980a9767e000c7717b246dbba387727dbd49dc676f`; Wallet Observer **25,192,036 bytes**, SHA-256 `3e7c7b1ffa6abd626264c4a8dd60882e0519bbe0a39821c12c2cd12a1b73bcdb`. Todas `1.4/5`, ZIP íntegro, firma debug v2, no release.
- La prueba nueva declara 15 aserciones; con las 38 previas quedan 53 declaradas en el bloque, todavía pendientes del runtime oficial Supabase/Postgres. No se ejecutaron pagos reales, despliegue, publicación ni instalación física.
