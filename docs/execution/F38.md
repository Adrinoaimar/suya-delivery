# F38 — Supabase remoto como única base de QA

Fecha: 24 de septiembre de 2026 (`America/Lima`)

## Verificación remota

- Producción Supabase: CLI informó `82` migraciones; última `20260924150000`, igual al repo. Las dos migraciones F37 ya están aplicadas.
- QA Supabase: proyecto `SUYA-STAGING-QA` está activo, pero `public.restaurants`, `public.orders`, `public.profiles` y el ledger de migraciones no existen aún.
- El dry-run remoto de migraciones QA falló antes de modificar datos: `28P01`, autenticación de `cli_login_postgres`. No se escribió en producción ni QA durante F38.
- PgTAP de producción ya pasó 18/18 en F37 dentro de transacción con `ROLLBACK`. Lint directo sigue pendiente de credencial SQL válida.

## Limpieza

- Quitados `supabase start/stop`, pruebas y lint `--local`, configuración de puertos/servicios Supabase locales, carga automática de semilla y flujos CI que iniciaban Supabase sobre Docker. El SQL del catálogo se conserva como fuente para el manifiesto de imágenes, fuera del flujo de migración/seed.
- `db:test` y `db:lint` ahora apuntan al proyecto Supabase QA. El flujo browser CI quedó en smoke UI con mocks explícitos; recorridos que creaban pedidos y perfiles quedaron retirados hasta preparar QA remoto.
- Se conservaron migraciones SQL versionadas, tests pgTAP, la configuración mínima necesaria para CLI/Edge Functions y almacenamiento de sesión/preferencias de navegador. No son una base local.
- No se desinstaló software del equipo. El repo ya no configura ni levanta Docker/Postgres local.

## Pendientes

- Restablecer autenticación SQL de QA, aplicar las migraciones versionadas y ejecutar ahí pruebas/lint remoto.
- El `push` a `main` dispara el flujo de publicación Cloudflare. No se empujó esta sesión ni se publicó; cambios siguen en `main` local.
