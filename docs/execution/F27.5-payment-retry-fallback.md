# F27.5 · Fallback de reintento Culqi

Fecha: 14 de septiembre de 2026 (`America/Lima`).

## Resultado

El commit `673a920` impide conservar una referencia Culqi después de un rechazo cuando el refresco server-side tampoco responde. El intento se presenta como fallido, sin referencia reutilizable, y el próximo toque debe crear un intento nuevo. También se corrigió el texto de privacidad para reflejar que se persiste un hash no reversible y solo se expone el sufijo de cuatro caracteres.

## Evidencia

- Local: 60 suites/249 pruebas, lint, typecheck, `git diff --check` y escaneo de secretos verdes.
- CI PR #36: DB `34873443644`, Android `34873443609`, iOS `34873443610`, browser `34873443608`, frontend/E2E `34873443611`; todos verdes.
- APK Rider, Back Office y Caja de `673a920` descargadas desde CI; `unzip -t` pasó en las tres.

## Gates pendientes

No hay llaves de prueba Culqi ni configuración completa de Supabase/Cloudflare disponible en el entorno. `verify:production` y `verify:cloudflare` rechazan publicar sin ellas. Faltan migraciones y Edge Functions reales, webhook, despliegue, smoke físico y dos cobros de S/30.
