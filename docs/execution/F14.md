# F14 — Checkpoint móvil reproducible y límites externos

Fecha: 9 de septiembre de 2026 (`America/Lima`)

## Resultado

- `main` quedó en `a3b33f3` con workflow Android reproducible (`mobile-android.yml`). El run
  `34392017329` compiló el APK debug unificado y publicó el artefacto CI.
- La verificación local con configuración pública canónica produjo bundles aislados de customer,
  rider y backoffice; no encontró mocks ni secretos.
- Donde Joel permanece en Supabase con dirección, teléfono, galería de cuatro cartas y 133 productos
  activos; los pedidos siguen deshabilitados hasta confirmar operación y logística.
- La app independiente Wallet Observer v7 tiene sincronización opcional en segundo plano hacia
  `ingest_wallet_observation`; conserva estado `unverified` y no confirma pagos automáticamente.

## Pruebas

- `npm run typecheck`: pasa.
- `npm run lint`: pasa sin warnings.
- `npm test -- --reporter=dot`: 25 archivos, 119 pruebas pasan.
- `npm run build:apps` con configuración Supabase/OSM canónica: pasa.
- `npm run verify:production`: 333 archivos, sin simulaciones ni secretos.
- `npm run security:secrets`: 459 archivos, sin patrones de secreto.
- `npm run verify:cloudflare` con los tres orígenes canónicos: pasa.
- `git diff --check`: pasa.
- `npm run db:lint`: no ejecutable localmente porque no hay Postgres Supabase en `127.0.0.1:54322`;
  queda delegado a CI.

## Gates externos y siguiente acción

- Google OAuth sigue apagado en Supabase: faltan Client ID/Secret y callbacks autorizados en Google
  Cloud/Supabase. No se inventan credenciales ni se guardan secretos en frontend.
- Cloudflare Pages conserva el workflow, pero falta `CLOUDFLARE_API_TOKEN` para publicar desde CI.
- APK es debug; distribución requiere keystore de release y prueba física.
- Primer comando al retomar: `gh run list --repo Adrinoaimar/suya-delivery --limit 10` y comprobar
  que el run de Android y el smoke web sigan verdes antes de tocar código.
