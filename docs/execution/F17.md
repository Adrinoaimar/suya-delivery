# Checkpoint F17 — Recuperación, activos de marca y gates

Fecha: 11 de septiembre de 2026 (`America/Lima`).

## Resultado

- Merge de la línea móvil con `main` resuelto conservando precarga de catálogo, cuentas de
  restaurantes, rediseño y activos de cartas.
- Andá Paya deja de usar un SVG recreado. La tarjeta y ficha usan la carta original como galería
  cuando falta logo; el backoffice permite cargar el archivo oficial.
- Suya Menús valida URLs de imágenes antes de insertarlas en CSS.
- Se añadió regresión de activos de marca y se actualizó la documentación de procedencia.

## Evidencia

- `npm run typecheck`: pasa.
- `npm run lint`: pasa.
- `npm run test`: 31 archivos, 141 pruebas pasan.
- `npm run build:apps` con backend Supabase/OSM: pasa.
- `npm run verify:production` con configuración de producción: 331 archivos, sin mocks ni secretos.
- `npm run build:mobile`: bundle móvil, Capacitor Android/iOS y plugins sincronizados.
- `npm run security:secrets`: 486 archivos, sin patrones de secreto.
- `git diff --check`: pasa.

## Gate externo

La compilación Gradle no pudo ejecutarse porque esta máquina no tiene Java ni `JAVA_HOME`. Firma
release, pgTAP/db lint remoto, OAuth físico y despliegue siguen siendo gates externos.

## Siguiente acción

Instalar o seleccionar JDK 21, compilar APK debug/release, instalar en Android físico y comprobar
catálogo, logos, Google OAuth y deep link. Confirmar con Andá Paya el archivo de logo final y subirlo
desde `Catálogo y publicación`.
