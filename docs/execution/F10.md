# Checkpoint F10 — Runner E2E multiapp

## Resultado

- Se añadió `scripts/browser-smoke.mjs` con Playwright, tres aplicaciones, móvil/tablet/escritorio,
  rutas protegidas, accesibilidad básica, overflow, `pageerror` y `prefers-reduced-motion`.
- Se añadió `test:e2e` y workflow `.github/workflows/e2e.yml`.
- CI inicia Supabase local real, obtiene su URL y clave pública, construye bundles Supabase, sirve
  las tres aplicaciones e instala Chromium real. La excepción `CI_E2E=true` acepta solo URL local;
  el gate productivo continúa rechazando URLs locales.

## Evidencia local

- Chrome real local: 9/9 casos de navegación y guards aprobados en 390×844, 768×1024 y 1440×900.
  Este smoke usa build local con configuración sintética y no demuestra todavía consulta Supabase.
- 18 archivos, 89 tests aprobados; typecheck, lint, secretos y YAML pasan.
- `npm ci --dry-run` reconoce Playwright y lockfile consistente.

## Pendiente

- Esperar CI E2E; la primera ejecución falló extrayendo variables locales y la segunda corrección
  cambió el parser a JSON + `jq`. No declarar backend E2E aprobado hasta verlo verde.
- Ejecutar contra Supabase dedicado y URLs Pages tras resolver gates externos.


