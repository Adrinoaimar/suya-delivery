# Checkpoint F0

## Objetivo

Permitir arquitectura productiva, proteger secretos, ejecutar CI en PR y dejar continuidad
verificable antes del refactor async.

## Rama y base

- Rama: `codex/functional-suya-loop`
- Base: `26831db`

## Cambios

- `AGENTS.md`: Supabase autorizado; mocks prohibidos en producción.
- `.gitignore` y `.env.example`: variables y exclusiones seguras.
- CI: checks en PR, permisos de despliegue mínimos por job y escaneo de secretos.
- Verificador de lanzamiento: falla mientras un bundle contenga simulaciones o configuración mock.
- ADR y `docs/STATE.md`: arquitectura y continuidad.
- README: sitio actual marcado como legado no productivo.

## Evidencia previa

- Typecheck: pasa.
- Lint: pasa.
- Tests: 7 archivos, 46 pruebas pasan.
- Build: pasa.
- Escaneo: 178 archivos, sin patrones de secreto.
- `git diff --check`: pasa.
- `verify:production`: falla correctamente y enumera los artefactos simulados que deben retirarse
  antes del lanzamiento.

## Pendiente externo de gobernanza

`main` no tiene branch protection. Se configurará al abrir el PR y conocer el nombre estable de los
checks requeridos, evitando bloquear el repositorio con un contexto inexistente.

## Siguiente acción

Ejecutar verificaciones F0, corregir hallazgos, commit. Después migrar contratos de servicio a async.
