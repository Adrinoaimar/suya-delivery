# F27.4 · Reintento seguro después de rechazo Culqi

Fecha: 14 de septiembre de 2026 (`America/Lima`).

## Resultado

El commit `73a1253` hace que `PaymentInstructions` consulte nuevamente el intento en Supabase cuando Culqi rechaza un token. La pantalla ya no conserva un estado local `pending`; el siguiente intento puede crear una orden/referencia nueva. Se agregó una regresión para tarjeta y botón `Reintentar pago`.

## Evidencia

- Local: 60 suites/248 pruebas, lint, typecheck, `git diff --check` y `build:apps` con configuración pública sintética.
- CI PR #36: DB `34871217318`, Android `34871217256`, iOS `34871217214`, browser `34871217191`, frontend/E2E `34871217316`; todos verdes.
- APKs Rider, Back Office y Caja fueron descargadas desde CI, pasaron `unzip -t` y permanecen como debug.
- Escaneo de secretos: sin secretos versionados.

## Gates pendientes

`verify:production` falla sin valores públicos reales; esto es correcto y evita publicar una demo. Todavía faltan configuración de Culqi/Supabase, migraciones y Edge Functions en el proyecto real, webhook, publicación Cloudflare, prueba física y dos pagos reales de S/30. No declarar la prueba productiva lista hasta cerrar esos gates.

## Siguiente acción

Con las credenciales de prueba cargadas por el responsable, ejecutar primero el preflight de Edge Functions, aplicar migraciones, publicar el build desde `main` y correr el checklist de dos Yape, QR Culqi, tarjeta y Lemon sin registrar secretos ni datos personales.
