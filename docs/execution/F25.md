# Checkpoint F25 — Estabilización de mapas y APKs por rol

## Resultado

- Se corrigió reconstrucción de Leaflet por lecturas GPS y redibujo innecesario de rutas en Cliente.
- Se añadió modo `Ver mapa completo` para Cliente/Rider, salida por Escape, bloqueo de scroll y safe
  area. Se conserva guía vial, rutas alternativas, rastro GPS e iconografía redondeada.
- El mapa móvil Cliente ocupa 54% de la vista. El proveedor de mapa cambia correctamente entre
  offline y online.
- Se corrigió un riesgo de roles: el build móvil unificado ya no se identifica como Cliente para
  instalar OTA; el actualizador queda fail-closed y requiere rol Cliente explícito.

## Cambios

- Commit final: `17ac6aa` en PR #36.
- Guía vial: OSRM/OpenStreetMap con instrucciones izquierda/derecha, alternativas, timeout y trazo
  fallback cuando el motor vial no responde.
- APKs Android debug publicadas por workflow `34740936708`, separadas por rol: Rider y Back Office.

## Evidencia

- Local: 46 suites, 204 tests, lint, typecheck, build aislado, escaneo de secretos, `git diff --check`
  y smoke responsive en tres viewports.
- CI: `browser`, `browser-e2e`, `build`, `debug`, `simulator` y `test` verdes.
- OSRM respondió HTTP 200 con geometría vial, pasos y CORS habilitado para el origen de Suya.
- Producción: los tres dominios responden HTTP 200; `/login` de panel muestra `Suya Operaciones` y
  una ruta protegida vuelve a ese login, no al Cliente.
- `npm run verify:production` rechazó el entorno local por variables productivas ausentes; no se
  presentó como aprobación de producción. `npm run security:secrets` pasó sin patrones sensibles.

## Pendientes

- PR #36 sigue abierto: fusionar y publicar requiere autorización explícita.
- Aplicar migración `restaurant_riders` en Supabase real y probar flujo con dos dispositivos físicos.
- Configurar endpoint OSRM gestionado/propio para producción con SLA; el endpoint público sirve para
  demo y validación, no garantiza disponibilidad operativa.
- Firma Android release pendiente.
